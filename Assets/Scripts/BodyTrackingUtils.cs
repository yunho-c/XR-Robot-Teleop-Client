using UnityEngine;

public class BodyTrackingUtils : MonoBehaviour
{
    public OVRSkeleton skeleton;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
